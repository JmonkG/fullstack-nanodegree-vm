useful links:

Para actualizar la version de pip que se usa para instalar psycopg2
pip install --upograde setuptools

Para evitar el error de segmentacion en python
instalar pkg guardado en dropbox sft


psql // comando para conectarse al servidor postgres

\c connect to database 
\dt para mostrar las tablas
\df para mostrar las funciones